name: Update Wiki

on:
  push:
    paths:
      - './Bomberman.wiki/wiki/**'
jobs:
  update-wiki:
    runs-on: ubuntu-latest
    name: Update wiki
    steps:
      - uses: OrlovM/Wiki-Action@v1
        with:
          path: './Bomberman.wiki/'
          token: ${{ secrets.GITHUB_TOKEN }}